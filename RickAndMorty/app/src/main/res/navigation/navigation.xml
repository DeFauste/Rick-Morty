<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/characters_frag">

    <fragment
        android:id="@+id/characters_frag"
        android:name="com.example.rickandmorty.Characters"
        android:label="fragment_characters"
        tools:layout="@layout/fragment_characters" />
    <fragment
        android:id="@+id/episodes_frag"
        android:name="com.example.rickandmorty.Episodes"
        android:label="fragment_episodes"
        tools:layout="@layout/fragment_episodes" />
    <fragment
        android:id="@+id/location_frag"
        android:name="com.example.rickandmorty.Location"
        android:label="fragment_location"
        tools:layout="@layout/fragment_location" />
</navigation>